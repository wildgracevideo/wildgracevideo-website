[dev]
   command = "npm run dev"
[build]
  command = "prisma generate && npm run build"
  publish = "dist"
  functions = 'functions' 

[[headers]]
  for = "/*"
    [headers.values]
    Access-Control-Allow-Origin = "*"
